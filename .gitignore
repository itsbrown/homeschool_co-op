node_modules
.worktree-*/
# Runtime timestamped JSON snapshots (new files only; does not remove existing tracked backups)
data/backups/*-*-*T*.json
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Backup files (may contain sensitive data)
data/backups/
data/backups/data/scheduled-payments.json
