node_modules
.worktree-*/
scratch/
# Runtime timestamped JSON snapshots (new files only; does not remove existing tracked backups)
data/backups/*-*-*T*.json

# Local file-storage dev state (mutates during dev / E2E seeds; server starts empty if missing)
data/users.json
data/locations.json
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
# Saved login session (contains tokens — never commit)
/playwright/.auth/parent.json

# Local Playwright / Supabase secrets (see .env.e2e.example)
.env.e2e

# Local dev secrets (see .env.example)
.env
.env.local

# Prod DB connection string for agent/manual investigation — NEVER commit
.env.prod

# Jest integration DB probe (written by globalSetup)
.jest-cache/