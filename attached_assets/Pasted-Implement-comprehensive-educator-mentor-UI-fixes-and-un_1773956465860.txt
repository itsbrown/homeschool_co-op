Implement comprehensive educator/mentor UI fixes and unification from scheduler audit. Merge Schedule and Lesson Plans into one view, enable block completion toggle, fix permissions, and address other gaps.

High-priority fixes:

1. Educator block completion toggle (EducatorWeeklySchedulePage.tsx)
   - Add checkbox to each block card (desktop table + mobile card)
   - Optimistic toggle: flip checked state, strikethrough title + opacity-60 on checked (use cn())
   - Use useMutation + apiRequest to PATCH /api/schedule-builder/week-plan-blocks/:id with { isCompleted: newValue }
   - Invalidate ["/api/schedule-builder/week-plans", weekPlanId] on success
   - Rollback on error + show toast error with useToast() from @/hooks/use-toast
   - Disabled when wb null
   - Console.log("Toggling block X to completed: true/false")

2. Fix educator read/write permissions (server/api/schedule-builder.ts)
   - Add 'educator', 'mentor', 'teacher' to requireRole on GET routes (skeletons, week-plans, blocks, history)
   - For PATCH /week-plan-blocks/:id and POST /week-plan-blocks/:id/complete:
     - Allow 'educator', 'mentor', 'teacher'
     - Add class check: const assignments = await storage.getEducatorClassAssignmentsByEducatorId(req.user?.id);
       if (!assignments.some(a => a.classId === plan.classId || plan.classId == null)) return 403;
   - Use req.user?.id (integer) for lookups

3. Merge Schedule + Lesson Plans (EducatorWeeklySchedulePage.tsx)
   - Remove "Lesson Plans" from educatorNavigationItems in EducatorAppShell.tsx (or comment out)
   - In grid: make block cards expandable (click to show full details)
   - Expanded: description, objectives (ul), lesson link (a), notes, groups, completion checkbox
   - Use state: const [expandedBlocks, setExpandedBlocks] = useState<Record<number, boolean>>({});
   - Toggle: setExpandedBlocks(prev => ({ ...prev, [block.id]: !prev[block.id] }))
   - Keep Week Note at top

Medium-priority fixes:
4. Centralize BLOCK_TYPE_COLORS (src/lib/blockColors.ts)
   - Export BLOCK_TYPE_COLORS (border-l classes) and BLOCK_TYPE_BADGE_COLORS (bg classes)
   - Import in ScheduleBuilderPage.tsx (use BADGE for badges), WeekPlannerPage.tsx (COLORS for borders), EducatorWeeklySchedulePage.tsx (COLORS for mobile card borders)
   - Remove local/copy-pasted mappings

5. Add time overlap + endTime validation (ScheduleBuilderPage.tsx)
   - In handleBlockSubmit: check new block time vs existing blocks on same dayOfWeek
   - Show toast error if overlap or endTime <= startTime
   - Use formatDate() from @/lib/utils for date display

6. AI unavailability fallback (ScheduleBuilderPage.tsx & WeekPlannerPage.tsx)
   - If aiAvailable false, show message: "AI generation is currently unavailable. Fill blocks manually or contact support."
   - For per-block AI buttons too

Apply safely. Use cn(), useRole() from RoleContext, full query keys (["/api/schedule-builder/week-plans", id]), useToast(), formatDate().
Prioritize: completion toggle + permissions first.
Show updated sections file-by-file, starting with EducatorWeeklySchedulePage.tsx and EducatorAppShell.tsx.
Add comments explaining changes.