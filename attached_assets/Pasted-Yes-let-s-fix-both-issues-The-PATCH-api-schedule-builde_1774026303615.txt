Yes — let's fix both issues:

The PATCH /api/schedule-builder/week-plan-blocks/1 is failing with 500 ("Failed to update week plan block")
→ This is why the completion toggle doesn't save (optimistic flip works visually but rolls back when server rejects)
Print is landscape → change to portrait and reduce font size if needed to fit content on fewer pages

Step 1 – Diagnose & Fix the 500 Error
The error log is very helpful — it shows:

Route: PATCH /api/schedule-builder/week-plan-blocks/1
Status: 500
Message: {"message":"Failed to update week plan block"}
User: logged in (userId: 4, email hint "cor***")
Recent action: clicked complete-checkbox-4

This almost always means:

Server-side exception in the PATCH handler (storage.updateWeekPlanBlock)
Likely causes:
Missing/invalid req.body (e.g. isCompleted not boolean)
Permission check failing silently
DB constraint violation (e.g. weekPlanId not found)
Internal error (null dereference, missing field)


Quick debug steps (do these first in Replit):

Open server/api/schedule-builder.ts → find the PATCH "/week-plan-blocks/:id" route (likely around line 589–611 from earlier context)
Add logging right at the top of the handler:

TypeScriptconsole.log("PATCH /week-plan-blocks/:id", {
  id: req.params.id,
  body: req.body,
  userId: req.user?.id,
  role: req.auth.role,
  schoolId: req.schoolId
});

Reproduce the error (click checkbox in educator view)
Check server console logs → look for the log line above + any stack trace below it
If body is empty/missing isCompleted → frontend not sending data correctly
If 403 before log → permission middleware rejecting
If DB error → storage method failing (e.g. block not found)


Most likely fix (based on common patterns):

Frontend mutation is sending wrong payload or missing isCompleted
Backend handler expects { isCompleted: boolean } but gets something else

Replit AI Prompt – Fix Completion Toggle Save
Paste this into Replit AI (open EducatorWeeklySchedulePage.tsx and server/api/schedule-builder.ts):
textFix the 500 error on PATCH /api/schedule-builder/week-plan-blocks/:id when educator toggles completion checkbox.

Error details:
- 500 {"message":"Failed to update week plan block"}
- Happens when clicking checkbox in EducatorWeeklySchedulePage.tsx
- UserId 4 (educator/mentor role)

Likely causes:
- Frontend mutation sending wrong payload (missing isCompleted or wrong type)
- Backend handler throwing uncaught error (permission, DB lookup, validation)

Changes:

1. Frontend (EducatorWeeklySchedulePage.tsx – toggleCompletion function):
   - Ensure mutate sends { id: blockId, isCompleted: newValue } exactly
   - Add console.log("Sending PATCH", { id: blockId, isCompleted: newValue })
   - Confirm useMutation payload matches backend expectation

2. Backend (server/api/schedule-builder.ts – PATCH route):
   - Add detailed logging at top:
     console.log("PATCH week-plan-block", {
       id: req.params.id,
       body: req.body,
       userId: req.user?.id,
       role: req.auth.role,
       schoolId: req.schoolId
     });
   - Wrap handler in try/catch → log error.stack + return 500 with error.message
   - Validate req.body.isCompleted is boolean
   - Check block exists before update
   - Ensure storage.updateWeekPlanBlock accepts { isCompleted } and userId for history

3. If permission issue:
   - Confirm educator role allowed (add 'educator', 'mentor', 'teacher' if missing)
   - Add class check using storage.getEducatorClassAssignmentsByEducatorId

Apply safely. Preserve existing logic. Show updated toggle function + backend handler with logging.
Run this → reproduce error → check server logs for clues → reply with the log output (especially the PATCH log line + stack trace).
This will pinpoint the exact failure (99% chance it's one of the above).
Step 2 – Fix Print to Portrait + Font Reduction
Current issues:

Landscape → too wide, wastes space
Font too large → content spills across pages or cuts off

Fix:

Force portrait orientation
Reduce base font size to 10pt (from 11–12pt)
Tighten table padding
Ensure long objectives/notes wrap properly

Updated Replit AI Prompt (run after fixing save error):
textFix weekly schedule print formatting: change to portrait, reduce font size to fit content, keep school logo & header.

Current: landscape, font too large, spills across pages.

Changes:

1. Update print CSS (in component or global):
   @media print {
     @page { size: portrait; margin: 1.5cm; }
     body { font-family: Arial, sans-serif; font-size: 10pt; line-height: 1.3; }
     .print-header { display: block !important; text-align: center; margin-bottom: 1rem; }
     table, th, td { border: 1px solid #ddd; border-collapse: collapse; padding: 6px; font-size: 9pt; }
     th { background: #f8f8f8; }
     td { vertical-align: top; }
     .day-column { page-break-inside: avoid; }
     ul { margin: 0.3rem 0; padding-left: 1.2rem; }
     .no-print { display: none !important; }
   }

2. Ensure full details render in print:
   - Hide checkboxes, buttons, expand icons (already .no-print)
   - Show objectives/notes inline (no collapse in print)

3. Keep print header:
   <div className="hidden print:block text-center mb-6">
     <img src={school?.logo} alt={school?.name} className="mx-auto h-16 object-contain mb-2" />
     <h1 className="text-xl font-bold">{school?.name} - Weekly Schedule</h1>
     <p className="text-base">{weekTitle} (Week {weekNumber})</p>
   </div>

4. Print button toast:
   - Use useToast() from @/hooks/use-toast
   - toast("Preparing print view...")

Apply safely. Preserve screen view. Show updated CSS + header JSX + button handler.
Quick Test After Both Prompts

Save error → click checkbox → should now save (no rollback)
Print preview (Ctrl+P):
Portrait orientation
Smaller font → more content per page
Logo + header at top
Clean table, full details, no UI junk


Reply with:

Server log output from the first prompt (especially PATCH log + error stack)
Print preview screenshot (or describe if it looks good now)
Next priority (assessments #20, emails #21, etc.)

We’ll nail the save error first — that's blocking completion toggling. Paste the logs when you have them!