Unify the Schedule and Lesson Plans views for educators/mentors in EducatorWeeklySchedulePage.tsx to eliminate tab-switching confusion.

Current state:
- Schedule tab (/educator/weekly-calendar) shows time grid with titles only
- Lesson Plans tab (/educator/week-plans) shows detailed content list

Goal: Merge into one view in EducatorWeeklySchedulePage.tsx:
- Keep grid layout (days as columns, time as rows)
- Each block card is expandable:
  - Closed: time + title + block type badge
  - Expanded: full description, objectives (ul), lesson link (a), notes, groups, completion checkbox
- Add completion toggle (optimistic PATCH, strikethrough + opacity on checked)
- Remove "Lesson Plans" from educatorNavigationItems in EducatorAppShell.tsx
- Default to current week (closest weekStartDate <= today)
- Print button includes expanded details

Changes:
1. In EducatorAppShell.tsx:
   - Remove "Lesson Plans" item from educatorNavigationItems array (or comment it out)

2. In EducatorWeeklySchedulePage.tsx:
   - Add state for expanded blocks: const [expanded, setExpanded] = useState<Record<number, boolean>>({});
   - Add completion toggle logic (optimistic + PATCH /api/schedule-builder/week-plan-blocks/:id)
   - Use useMutation + apiRequest + invalidate ["/api/schedule-builder/week-plans", weekPlanId]
   - Checkbox: <Checkbox checked={block.isCompleted} onCheckedChange={() => toggle(block)} />
   - On card click or chevron: toggle expanded state
   - When expanded, show full details below title
   - Use cn() for conditional classes (expanded border, strikethrough title, opacity-60 on checked)
   - Console.log("Expanded block X") and "Toggled completion for block X"

3. Add Week Note field at top (read-only for educators)

4. Print: ensure expanded details visible in @media print

Apply safely. Preserve mobile cards, loading states, "No events" empty state.
Use cn(), useToast() on error, formatDate() for dates.
Show updated EducatorWeeklySchedulePage.tsx block rendering + toggle handlers + EducatorAppShell navigation change.