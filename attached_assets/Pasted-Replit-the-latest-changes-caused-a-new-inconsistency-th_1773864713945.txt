Replit, the latest changes caused a new inconsistency: the ASA Assistant / concierge sidebar now shows **$6805.00** PAYMENTS DUE (sum of $3875.00 + $2930.00 overdue items), which is even farther from the true outstanding amount (~$4,050 from PaymentManagement). Available Credits is still $422.00 in concierge (with "manual: $358.50" sub-line, suggesting pending credits are included). Net due is $6383.00, which is wrong.

This is worse than before — the "true amount" parents owe is the effectiveBalance total from enrollments ($4,050 gross, ~$3691.50 net after $358.50 credits). The concierge must match that.

Apply these exact fixes (no new features, no UI additions, no unrelated changes):

1. server/api/parent-concierge.ts (context endpoint and check_payments)
   - Force totalDue to exactly match PaymentManagement's outstanding logic:
     - Fetch enrollments using the same storage method as /api/parent/enrollments
     - Deduplicate by classId-childId, picking latest
     - Sum effectiveBalance for those with effectiveBalance > 0
     - Ignore scheduled payments for totalDue — use only enrollment effectiveBalance
   - This should make concierge PAYMENTS DUE = $4,050

2. Fix credits to $358.50 everywhere
   - In storage.getTotalAvailableCredits (or /api/parent/credits): Exclude pending credits; only approved/partially_used with remaining > 0 and not expired
   - In concierge frontend: Use totalAvailableCents from /api/parent/credits (same as PaymentManagement)
   - Remove any "manual" sub-line if it's causing confusion — show only the filtered total

3. PaymentManagement.tsx
   - Ensure Net Due bar uses the filtered credits ($358.50)
   - If overdue items in concierge are from scheduled payments, hide or relabel them as "Next Installments" (not part of total due)

After changes:
- Restart/rebuild
- Hard refresh
- Tell me the new numbers in concierge: PAYMENTS DUE, Available Credits, Net Due
- And confirm PaymentManagement still shows $4,050 outstanding / $358.50 credits / ~$3,691.50 net

Do only these — focus on syncing to effectiveBalance and filtered credits.