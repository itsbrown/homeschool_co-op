Implement Task #22 - Fix Week Planner Checkbox Toggle exactly as described, with project-specific conventions.

Goals (same as original):
- Checkboxes toggle both ways with immediate visual feedback (optimistic)
- Checked blocks show strikethrough title + reduced opacity
- Server PATCH confirms; rollback on failure
- Disabled when wb is null
- Console.log on every toggle

Important project conventions to follow:
- Use full API path query keys: ["/api/schedule-builder/week-plans", selectedWeekPlanId] for invalidation
- Never use short-hand keys like ['week-plan', id]
- Use cn() helper from class-variance-authority for conditional classes
- Prefer optimistic updates + rollback pattern in mutations
- Add console.log for debugging as specified

Specific changes in WeekPlannerPage.tsx:

1. Add optimistic overrides state:
   const [completionOverrides, setCompletionOverrides] = useState<Record<number, boolean>>({});

2. Add toggleBlockCompletion function:
   const toggleBlockCompletion = (wb: WeekPlanBlock | null) => {
     if (!wb) {
       console.log("Toggle attempted on null wb — ignored (no block exists)");
       return;
     }

     const blockId = wb.id;
     const currentValue = completionOverrides[blockId] ?? wb.isCompleted;
     const newValue = !currentValue;

     console.log(`Toggling completion for block ${blockId}: ${currentValue ? 'checked' : 'unchecked'} → ${newValue ? 'checked' : 'unchecked'}`);

     // Optimistic update
     setCompletionOverrides(prev => ({ ...prev, [blockId]: newValue }));

     // Server mutation
     toggleCompletionMutation.mutate(
       { id: blockId, isCompleted: newValue },
       {
         onSuccess: () => {
           // Invalidate with correct full path key
           queryClient.invalidateQueries({
             queryKey: ["/api/schedule-builder/week-plans", currentWeekPlanId]
           });
           // Clean up override
           setCompletionOverrides(prev => {
             const copy = { ...prev };
             delete copy[blockId];
             return copy;
           });
         },
         onError: (err) => {
           console.error(`Toggle failed for block ${blockId}`, err);
           // Rollback
           setCompletionOverrides(prev => {
             const copy = { ...prev };
             delete copy[blockId];
             return copy;
           });
         }
       }
     );
   };

3. Replace any existing completeBlockMutation with toggleCompletionMutation:
   - Use useMutation from @tanstack/react-query
   - PATCH /api/schedule-builder/week-plan-blocks/:id with { isCompleted }

4. Update checkbox in block render loop:
   <Checkbox
     checked={completionOverrides[wb?.id ?? 0] ?? wb?.isCompleted ?? false}
     onCheckedChange={() => toggleBlockCompletion(wb)}
     disabled={!wb}
   />

5. Add visual feedback for checked state (use cn()):
   In the block title element:
   className={cn(
     "text-sm font-medium",
     (completionOverrides[wb.id] ?? wb.isCompleted) && "line-through opacity-60"
   )}

6. Remove any old one-way completeBlockMutation usage

Apply safely in WeekPlannerPage.tsx only. Preserve grid layout, Add/AI/Edit/History/Delete buttons, anchor labels, etc.
Add comments explaining optimistic toggle, rollback, and query key convention.
Show updated sections: state declaration, toggle function, checkbox + title render.