Replit, implement task #15 "Fix Concierge Payment Total & Credits" exactly as described. Apply only the three numbered tasks — no new UI elements, no logging, no unrelated changes, no modifications to PaymentManagement.tsx logic.

Current symptoms (latest screenshots):
- Concierge sidebar: PAYMENTS DUE = $6,805 (inflated), Available Credits = $422 (includes held/pending credits), Net due after credits = $6,383
- Payments page (correct): Outstanding Balance = $4,050, Available Credits = $358.50, Net Due ≈ $3,691.50
- Overdue sub-items in concierge show full enrollment balances repeated, not individual installment amounts

Apply these exact fixes:

1. server/api/parent-concierge.ts (/context endpoint and check_payments handler)
   - Replace the current enrollment fetch + dedup + balance sum with the **exact same logic** as /api/parent/enrollments (server/api/parent.ts ~lines 370–413):
     - Fetch via storage.getProgramEnrollmentsByParent(user.id)
     - Enrich each enrollment with effectiveBalance = Math.max(0, totalCost - totalPaid - compAmountCents)
     - Filter out terminal statuses: cancelled, waitlist, withdrawn, failed, completed
     - Deduplicate by classId-childId, keeping the latest enrollment (compare createdAt or updatedAt)
     - totalDue = sum(effectiveBalance for e where effectiveBalance > 0)
   - This should make concierge PAYMENTS DUE = $4,050

2. server/api/parent-concierge.ts (same endpoint)
   - Replace any manual credit summation (approved/partially_used reduce) with:
     await storage.getTotalAvailableCredits(user.id)
   - This uses the canonical method that already subtracts held credits (in-progress checkouts), so Available Credits becomes $358.50
   - Remove any sub-breakdown lines in the response (e.g. "manual: $358.50") — return only the single total from getTotalAvailableCredits
   - Frontend (ParentConciergePage.tsx) should now display this single value without manual lines

3. server/api/parent-concierge.ts (upcomingPayments / overdue sub-items construction)
   - For each scheduled payment in upcomingPayments array:
     - Set item.amount = payment.amount (the scheduled installment amount field)
     - Do NOT set it to enrollment.effectiveBalance or remainingBalance
   - This ensures overdue lines show correct individual amounts (e.g. $X due on date Y) instead of repeating the full enrollment balance

After changes:
- Rebuild / restart the app
- Hard refresh both Payments page and main dashboard (concierge sidebar)
- Reply with the new values shown in concierge sidebar:
  - PAYMENTS DUE
  - Available Credits
  - Net due after credits (if shown)
- Confirm Payments page still shows:
  - Outstanding Balance $4,050
  - Available Credits $358.50
  - Net Due ≈ $3,691.50

Do only these three fixes. Keep diffs minimal — copy logic from parent.ts where possible.