Implement combined educator UI improvements: enable block completion toggle in EducatorWeeklySchedulePage.tsx and fix permission gaps + navigation confusion.

Fixes based on audit:

1. Educator block completion toggle (EducatorWeeklySchedulePage.tsx)
   - Add checkbox to each block card (same style as WeekPlannerPage)
   - Optimistic toggle: flip checked state, show strikethrough title + opacity-60 on checked
   - Use useMutation + apiRequest to PATCH /api/schedule-builder/week-plan-blocks/:id with { isCompleted: newValue }
   - Invalidate ["/api/schedule-builder/week-plans", weekPlanId] on success
   - Rollback optimistic state + show toast error on failure (use useToast() from @/hooks/use-toast)
   - Disabled when wb null (no block exists)
   - Console.log("Toggling block X to completed: true/false")

2. Permission gaps (server/api/schedule-builder.ts)
   - Relax GET routes for skeletons/week-plans/blocks to allow 'educator', 'mentor', 'teacher' roles (add to requireRole array)
   - For write routes (PATCH/DELETE blocks): add check:
     const assignments = await storage.getEducatorClassAssignmentsByEducatorId(req.user?.id);
     const hasAccess = assignments.some(a => a.classId === skeleton.classId) || skeleton.classId == null;
     if (!hasAccess && !['schoolAdmin', 'admin', 'superAdmin'].includes(req.auth.role)) return 403;
   - Use req.user?.id (integer) for all user lookups

3. Remove "Lesson Plans" tab confusion (EducatorAppShell.tsx or layout file)
   - In educatorNavigationItems array, remove or comment out the "Lesson Plans" item
   - Make "Schedule" the single source of truth for mentors

4. Current week auto-selection
   - Use formatDate() from @/lib/utils for any date display/comparison
   - Select closest weekStartDate <= today, fallback to most recent

Apply safely across files. Use cn() for classes, useRole() from RoleContext for frontend checks, full API path query keys.
Prioritize: completion toggle + read/write permissions first.
Show updated sections one file at a time, starting with EducatorWeeklySchedulePage.tsx.
Add comments explaining role checks, optimistic toggle, and navigation change.