Replit, the previous changes improved things but these inconsistencies still exist (from latest screenshots):

- Concierge sidebar "PAYMENTS DUE" = $5250 (scheduled installments total)
- PaymentManagement "Outstanding Balance" card = $4050 (correct enrollment effective balance total)
- PaymentManagement "Upcoming Payments" card = $5250
- Available Credits: $422 in PaymentManagement summary/credits tab/Net Due bar vs $358.50 in concierge
- Outstanding Enrollments list sums to $3875 (missing ~$175, likely the membership fee not shown in the list)

Please apply **only** these targeted fixes — no new UI elements, no logging, no unrelated changes:

1. server/api/parent-concierge.ts (around lines 740–800)
   - In the context endpoint response (and in check_payments if it computes totalDue separately):
     - Change totalDue calculation to use enrollment.effectiveBalance (the field already returned by /api/parent/enrollments) instead of manually recomputing totalCost - totalPaid - compAmountCents.
     - Use: effectiveBalance ?? Math.max(0, totalCost - totalPaid - (compAmountCents || 0))
     - Keep deduplication by classId-childId if already present.
     - This should make concierge PAYMENTS DUE become $4050.

2. server/storage.ts (around line 669) — fix getTotalAvailableCredits
   - Standardize the logic to exactly match what concierge currently uses manually:
     - Only include credits where status === 'approved' || status === 'partially_used'
     - Only if (creditAmountCents - usedAmountCents) > 0
     - Only if !expiresAt || expiresAt > now
   - This makes /api/parent/credits → totalAvailableCents return 35850 cents ($358.50)
   - Do NOT change the full credits array return — only the totalAvailableCents sum.

3. client/src/components/payments/PaymentManagement.tsx
   - Summary cards (around 1338–1403):
     - Change Available Credits display + Net Due bar to use creditsData?.totalAvailableCents ?? 0 (so it shows $358.50 everywhere)
   - Outstanding Enrollments list vs card total mismatch (around 1573–1590 vs card):
     - Option A (simpler & recommended): Remove membershipOutstanding from the "Outstanding Balance" card total.
       - Let card total = only sum of program enrollment effectiveBalances (matches the list below)
       - If membership is still owed, add a small separate line in the list: "Annual Membership · $175.00" (or similar)
     - OR Option B: Add membership as a line item in the Outstanding Enrollments list with clear label "Annual Membership Fee"
     - Goal: card total MUST exactly equal the visible sum of items in the list below it.
   - Upcoming Payments card (around 1494–1515):
     - Change title from "Upcoming Payments" to "Next Scheduled Installments"
     - Keep showing the $5250 total and the list of future payments — just make the label clear so it's not mistaken for total owed.

After changes:
- Restart / rebuild
- Tell me the new values you see in:
  - Concierge sidebar → PAYMENTS DUE
  - PaymentManagement → Outstanding Balance card
  - PaymentManagement → Available Credits (both places)
  - PaymentManagement → Net Due
  - PaymentManagement → sum visible in Outstanding Enrollments list

Do only the above — keep changes minimal and focused on consistency.