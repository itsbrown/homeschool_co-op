Replit — please implement task #13 "Fix Payment Number Consistency" exactly as described below. Follow the "Done looks like" criteria strictly. Apply only the four numbered tasks listed — no extra UI elements, no logging, no unrelated refactoring, no admin-view changes, no payment-plan logic changes.

Skills / patterns to follow: asa-payment-patterns, asa-database-patterns, asa-credit-system

Current symptoms (from latest screenshots):
- Concierge sidebar PAYMENTS DUE = $5250 (still using scheduled payments or manual recompute)
- PaymentManagement Outstanding Balance card = $4050
- PaymentManagement Available Credits = $422 (should be $358.50)
- Outstanding Enrollments list sums to ~$3875 (missing ~$175 membership)
- Net Due bar uses wrong credits number

Apply these exact changes:

1. server/api/parent-concierge.ts (lines ~365–415 and ~740–800)
   • In the context endpoint response object and in the check_payments handler:
     • Replace any manual totalDue = sum( totalCost - totalPaid - compAmountCents ) calculation
     • Use enrollment.effectiveBalance directly (PostgreSQL generated column returned by storage.getProgramEnrollmentsByParent)
     • Fallback only if missing: Math.max(0, (enrollment.totalCost ?? 0) - (enrollment.totalPaid ?? 0) - (enrollment.compAmountCents ?? 0))
     • Keep existing deduplication by classId-childId if present
     • Goal: concierge PAYMENTS DUE becomes $4050

2. server/storage.ts (~line 669) — IStorage.getTotalAvailableCredits implementation
   • Update the method to compute totalAvailableCents using the canonical asa-credit-system filter:
     - status === 'approved' || status === 'partially_used'
     - (creditAmountCents - usedAmountCents) > 0
     - !expiresAt || expiresAt > current timestamp
   • Do NOT modify the returned credits array (full list stays the same)
   • Only change how totalAvailableCents is calculated
   • This makes /api/parent/credits return totalAvailableCents = 35850 ($358.50)

3. client/src/components/payments/PaymentManagement.tsx
   • Summary cards section (~1338–1403):
     • Change available credits value (and value used in Net Due bar) to creditsData?.totalAvailableCents ?? 0
     • This fixes Available Credits to $358.50 and Net Due to correct value (~$3691.50)
   • Outstanding Balance card vs Outstanding Enrollments list (~1553–1590):
     • Remove membershipOutstanding from the total used for the "Outstanding Balance" card
     • So card total = only sum of program enrollment effectiveBalances
     • Add membership as a separate line at the bottom of the Outstanding Enrollments list:
       Example: 
       <div className="…">Annual Membership Fee · {formatCurrency(membershipOutstanding)}</div>
     • Goal: card total exactly = visible sum of all lines in the list below it

4. client/src/components/payments/PaymentManagement.tsx (~1494–1515)
   • Change the title of the "Upcoming Payments" summary card to:
     "Next Scheduled Installments"
   • Keep the dollar amount ($5250) and the list of future payments unchanged — only rename the card title

After applying:
- Rebuild / restart the app
- Hard refresh both the main dashboard (concierge) and Payments page
- Reply with the new values shown in:
  • Concierge sidebar → PAYMENTS DUE
  • PaymentManagement → Outstanding Balance card
  • PaymentManagement → Available Credits (summary card & credits tab)
  • PaymentManagement → Net Due (in the bar)
  • PaymentManagement → total visible in Outstanding Enrollments list (including membership line if added)

Do only these four changes. Keep diffs as small as possible.