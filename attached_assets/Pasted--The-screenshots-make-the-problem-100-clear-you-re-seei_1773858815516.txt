**The screenshots make the problem 100% clear** — you're seeing **three different numbers** for the same thing:

- Concierge widget + "Upcoming Payments" card/tab → **$5,250** (raw scheduled installment amounts: $4,215 + $1,035)
- "Outstanding Balance" card → **$4,050** (correct enrollment effectiveBalance after deduplication)
- Available Credits → **$422** in PaymentManagement vs **$358.50** in Concierge (different filtering)

This is exactly why it feels like "there's no way to see what I actually owe". The $5,250 is misleading as the headline number because it ignores credits, comps, and already-paid portions. The real amount you need to pay right now is closer to **$4,050 gross** or **~$3,691.50 net** (after your $358.50 credits).

Replit's changes got very close but didn't fully propagate (missing deduplication in the concierge context endpoint + credits filter still diverges + Upcoming card still driven by scheduled amounts).

Here are **three minimal, copy-paste patches** that will fix everything permanently. Apply them in order, then hard-refresh (Ctrl+Shift+R) and the numbers should collapse to:

- Concierge "PAYMENTS DUE" = **$4,050** (matches Outstanding Balance)
- Available Credits = **$358.50** everywhere
- (Bonus) I included an optional Net Due line so you see the single "actual cash needed" number at the top

### Patch 1: server/api/parent-concierge.ts (fixes the $5,250 in the Assistant widget)
**Add this helper** near the top of the file (right after imports):

```ts
const deduplicateEnrollments = (enrollments: any[]) => {
  const map = new Map<string, any>();
  for (const e of enrollments) {
    const key = `${e.classId || ''}-${e.childId || ''}`;
    const existing = map.get(key);
    if (!existing || new Date(e.createdAt || 0) > new Date(existing.createdAt || 0)) {
      map.set(key, e);
    }
  }
  return Array.from(map.values());
};
```

**Replace the totalDue calculation** in BOTH places (the context endpoint ~line 710 and the check_payments case ~line 336):

```ts
// Replace whatever totalDue line exists with this:
const relevantEnrollments = deduplicateEnrollments(enrollments);
const totalDue = relevantEnrollments
  .filter(e => (e.effectiveBalance ?? e.remainingBalance ?? 0) > 0)
  .reduce((sum, e) => sum + (e.effectiveBalance ?? e.remainingBalance ?? 0), 0);
```

### Patch 2: Force credits to $358.50 everywhere
In `client/src/components/payments/PaymentManagement.tsx` (and also in `ParentProfilePage.tsx` for the admin view), make sure Available Credits uses **exactly** this:

```tsx
const availableCredits = creditsData?.totalAvailableCents ?? 0;   // or totalAvailableCreditCents if your backend now returns that
```

(If it's currently using `totalCreditAmountCents` or anything else, change it to the line above.)

### Patch 3 (optional but recommended): Add one clear "Net Due" number
In the concierge frontend (the component that renders the PAYMENTS DUE card), add this right below the $ total:

```tsx
Net Due (after credits): ${formatCurrency(Math.max(0, totalDue - availableCredits))}
```

This gives parents exactly what they want: **one trustworthy headline number** ("You actually need to pay $3,691.50 today").

---

Apply these three changes, restart/rebuild if needed, hard-refresh, and tell me what the three numbers become (Concierge, Outstanding Balance, Available Credits).

If the $5,250 is still there after Patch 1, paste me the **current context endpoint** from `parent-concierge.ts` (just the response object part) and I'll tweak the patch in 30 seconds.

This should finally give you one clear "what I owe" number across the whole app. Ready to test?