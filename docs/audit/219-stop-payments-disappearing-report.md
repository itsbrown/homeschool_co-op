# Stop payments from silently disappearing — Audit Report

- **Task ref:** #219 (backfilled by #240)
- **Code SHA verified:** `b244edff4324e371354e871c8d9b8327b8bf9ed6`
- **Commit:** `b244edff4324e371354e871c8d9b8327b8bf9ed6 2026-05-08 15:33:25 +0000 Establish a standard for durable audit artifact storage`
- **Working tree:** clean (`git --no-optional-locks status -s` returned no output at 2026-05-08T15:43:26Z, after evidence run; the only file added in this task is this report itself, generated last)
- **Report generated:** 2026-05-08T15:44:00Z
- **Generated by:** task-agent (Replit Agent, Task #240 backfill)

> **Note on backfill scope:** This report is verification-only. No production code, tests, or schema were modified. All evidence below was produced against the SHA pinned above by exercising the existing test/harness endpoints and the live regression suite at `server/tests/integration/payment-flow/cart-pi-persistence-regression.test.ts`.

> **MUST-PASS CLOSE CLAUSE:** This report contains raw command output, raw SQL/query results, and raw API/test responses for every required section. Every block in Sections 1–3 is timestamped (ISO-8601, either via `$ date -u` capture or via the inline `ts` field returned by the harness/webhook endpoints).

---

## Section 0 — Code version pin (mandatory header)

```
$ date -u
Fri May  8 03:40:59 PM UTC 2026

$ git rev-parse HEAD
b244edff4324e371354e871c8d9b8327b8bf9ed6

$ git --no-optional-locks log -1 --format='%H %ci %s' HEAD
b244edff4324e371354e871c8d9b8327b8bf9ed6 2026-05-08 15:33:25 +0000 Establish a standard for durable audit artifact storage

$ git --no-optional-locks status -s
(no output — clean)
```

All evidence below was captured between `2026-05-08T15:43:14Z` and `2026-05-08T15:43:32Z` against this SHA.

---

## Section 1 — Property 1: successful events persist

A signed `payment_intent.succeeded` webhook was posted to `POST /api/stripe/webhook`.
The persisted row was then read back via `GET /api/test/stripe-payment-by-event/:eventId`.

**Webhook response (raw):**

```
ts=2026-05-08T15:43:14.779Z
HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":30}
```

**SQL read-back (raw response from `/api/test/stripe-payment-by-event/...`, which runs `SELECT id, stripe_event_id, payment_intent_id, amount, status, idempotency_key, created_at FROM stripe_payment_history WHERE stripe_event_id = $1`):**

```
ts=2026-05-08T15:43:14.797Z
HTTP 200
{
  "count": 1,
  "rows": [
    {
      "id": 30,
      "stripeEventId": "evt_test_240_p1p2_pi_test_240_1778254994778_8876",
      "paymentIntentId": "pi_test_240_1778254994778_8876",
      "amount": 10000,
      "status": "succeeded",
      "idempotencyKey": "pi_succeeded:evt_test_240_p1p2_pi_test_240_1778254994778_8876",
      "createdAt": "2026-05-08T15:43:14.790Z"
    }
  ]
}
```

**Verdict:** persistedRowId in the 200 body (`30`) matches the single DB row's `id` (`30`); both axes (`stripe_event_id`, `payment_intent_id`) are populated; `idempotency_key` is `pi_succeeded:<event.id>` per the handler contract. ✅

---

## Section 2 — Property 2: idempotent replay

Same signed event re-delivered to the webhook endpoint.

**Replay webhook response (raw):**

```
ts=2026-05-08T15:43:14.802Z
HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"duplicate":true,"persistedRowId":30}
```

**Row count after replay (raw):**

```
ts=2026-05-08T15:43:14.808Z
HTTP 200
{
  "count": 1,
  "rows": [
    {
      "id": 30,
      "stripeEventId": "evt_test_240_p1p2_pi_test_240_1778254994778_8876",
      "paymentIntentId": "pi_test_240_1778254994778_8876",
      "amount": 10000,
      "status": "succeeded",
      "idempotencyKey": "pi_succeeded:evt_test_240_p1p2_pi_test_240_1778254994778_8876",
      "createdAt": "2026-05-08T15:43:14.790Z"
    }
  ]
}
```

`SELECT COUNT(*) FROM stripe_payment_history WHERE stripe_event_id = '<id>'` is `1` (the harness endpoint surfaces this as the `count` field; the underlying query is `WHERE stripeEventId = $1` against the same table — the equivalent COUNT is therefore `1`). The replay returned the **same** persistedRowId (`30`).

**DB-level unique constraint — schema source (`shared/schema.ts:953-961`):**

```ts
export const stripePaymentHistory = pgTable("stripe_payment_history", {
  id: serial("id").primaryKey(),
  userId: integer("user_id").notNull().references(() => users.id, { onDelete: 'cascade' }),

  // Stripe identifiers
  paymentIntentId: text("payment_intent_id").notNull().unique(), // Stripe payment_intent ID
  // Task #219: stripe_event_id is the durable idempotency key for webhook events.
  // Unique to guarantee race-safe single-row persistence on replay.
  stripeEventId: text("stripe_event_id").unique(),
```

**DB-level unique constraint — live `\d stripe_payment_history` dump (captured at 2026-05-08T15:43:26Z, abridged to indexes):**

```
Indexes:
    "stripe_payment_history_pkey" PRIMARY KEY, btree (id)
    "idx_stripe_payment_history_customer_id" btree (customer_id)
    "idx_stripe_payment_history_user_id" btree (user_id)
    "stripe_payment_history_idempotency_idx" UNIQUE, btree (idempotency_key) WHERE idempotency_key IS NOT NULL
    "stripe_payment_history_idempotency_key_key" UNIQUE CONSTRAINT, btree (idempotency_key)
    "stripe_payment_history_payment_intent_id_key" UNIQUE CONSTRAINT, btree (payment_intent_id)
    "stripe_payment_history_stripe_event_id_key" UNIQUE CONSTRAINT, btree (stripe_event_id)
```

The `stripe_payment_history_stripe_event_id_key` UNIQUE CONSTRAINT is the DB-level guarantee that no replay can ever insert a second row keyed by the same `stripe_event_id`. ✅

---

## Section 3 — Property 3: persistence failure → 5xx

A dev-only fault-injection header (`x-task-219-fault-inject-persistence: true`) is honored by the handler to simulate a transient DB failure during the persistence claim. The same signed event is delivered twice under fault, then once more after the fault clears.

**First delivery under fault (raw):**

```
ts=2026-05-08T15:43:15.213Z
HTTP 500
{"error":"Webhook processing failed","event_type":"payment_intent.succeeded","message":"TASK_219_FAULT_INJECT_PERSISTENCE: synthetic DB failure (test header)"}
```

**Row count after first fault (raw):**

```
ts=2026-05-08T15:43:15.219Z
HTTP 200
{"count":0,"rows":[]}
```

**Stripe retry (still under fault) — raw:**

```
ts=2026-05-08T15:43:15.223Z
HTTP 500
{"error":"Webhook processing failed","event_type":"payment_intent.succeeded","message":"TASK_219_FAULT_INJECT_PERSISTENCE: synthetic DB failure (test header)"}
```

**Row count after second fault (raw):**

```
ts=2026-05-08T15:43:15.230Z
HTTP 200
{"count":0,"rows":[]}
```

**Recovery delivery (fault cleared) — raw:**

```
ts=2026-05-08T15:43:15.234Z
HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":31}
```

**Row count after recovery (raw):**

```
ts=2026-05-08T15:43:15.244Z
HTTP 200
{"count":1,"rows":[{"id":31,"stripeEventId":"evt_test_240_p3_pi_test_240_1778254995213_777542","paymentIntentId":"pi_test_240_1778254995213_777542","amount":10000,"status":"succeeded","idempotencyKey":"pi_succeeded:evt_test_240_p3_pi_test_240_1778254995213_777542","createdAt":"2026-05-08T15:43:15.238Z"}]}
```

**Verdict:** the handler returned 5xx on persistence failure (Stripe will retry — silent success is impossible), the in-memory dedup cache did NOT swallow the retried delivery (the second 5xx proves it), and once the fault cleared the very next delivery persisted exactly one row. The error is not swallowed: it propagates through the typed `PersistenceClaimError` introduced at `server/webhook-handler.ts:210-216` to the outer `try/catch` and surfaces as HTTP 500. ✅

---

## Section 4 — Property 4: observable skip paths (no silent omissions)

Skip-branch enumeration of the `case 'payment_intent.succeeded':` arm in `server/webhook-handler.ts`. Each branch records a structured entry through `recordTask219Skip` (defined at `server/lib/task219SkipLog.ts`) alongside its `console.warn`. The runtime skip buffer was queried via `GET /api/test/task-219-skips/:eventId` after each delivery to prove the WARN actually fires.

| # | Skip reason                                  | Source location                          | Coverage |
|---|----------------------------------------------|------------------------------------------|----------|
| 1 | `missing_parent_email`                       | `server/webhook-handler.ts:689-699`      | (a) WARN observed at runtime — see Branch 3+4 evidence below |
| 2 | `cart_checkout_metadata_signal`              | `server/webhook-handler.ts:721-735`      | (a) WARN observed — Branch 1 evidence below |
| 3 | `stripe_api_checkout_session_match` (fault-inject path) | `server/webhook-handler.ts:746-766` | (a) WARN observed — Branch 5 evidence below |
| 4 | `stripe_api_checkout_session_match` (real `stripe.checkout.sessions.list` match) | `server/webhook-handler.ts:767-798` | **(b) UNREACHABLE IN TEST** — requires a live Stripe-managed Checkout Session whose own metadata signals cart_checkout. The fault-inject branch (#3 above) exercises the identical skip-emit path. **Tracked: covered by task #239.** |
| 5 | `checkout_session_completed_already_owns`    | `server/webhook-handler.ts:805-821`      | (a) WARN observed — Branch 3+4 evidence below |
| 6 | `unhandled_payment_type`                     | `server/webhook-handler.ts:1370-1376`    | (a) WARN observed — Branch 2 evidence below |

The helper in use is `server/lib/task219SkipLog.ts` (imported at `server/webhook-handler.ts:22` and invoked at every recorded site enumerated above; bounded ring buffer of 256 entries, no-op in production).

### Branch 1 — `cart_checkout_metadata_signal` (raw runtime skip entry)

```
webhook ts=2026-05-08T15:43:15.656Z  HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":32}

skips ts=2026-05-08T15:43:15.666Z  HTTP 200
{"entries":[{"ts":1778254995664,"eventId":"evt_test_240_p4_cartmeta_pi_test_240_1778254995656_975030","eventType":"payment_intent.succeeded","paymentIntentId":"pi_test_240_1778254995656_975030","reason":"cart_checkout_metadata_signal","metadataKey":"paymentType","metadataValue":"cart_checkout","persistedRowId":32}]}
```

### Branch 2 — `unhandled_payment_type` (raw runtime skip entry)

```
webhook ts=2026-05-08T15:43:16.132Z  HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":33}

skips ts=2026-05-08T15:43:16.312Z  HTTP 200
{"entries":[{"ts":1778254996309,"eventId":"evt_test_240_p4_unhandled_pi_test_240_1778254996132_766208","eventType":"payment_intent.succeeded","paymentIntentId":"pi_test_240_1778254996132_766208","reason":"unhandled_payment_type","metadataKey":"paymentType","metadataValue":"task_240_unknown_xyz","persistedRowId":33}]}
```

### Branches 3 + 5 — `missing_parent_email` then `checkout_session_completed_already_owns` (raw runtime skip entries)

A pre-seeded `stripe_payment_history` row keyed by `idempotency_key='checkout:<sessionId>'` was inserted via `POST /api/test/seed-checkout-owned-pi`, then a signed PI **without** `parentEmail` was delivered. Both skip branches fire in sequence on the same event:

```
seed POST /api/test/seed-checkout-owned-pi  HTTP 200
{"id":34,"sessionId":"cs_test_seed_rI_xHoGe"}

webhook ts=2026-05-08T15:43:16.741Z  HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":34}

skips ts=2026-05-08T15:43:16.884Z  HTTP 200
{"entries":[
  {"ts":1778254996745,"eventId":"evt_test_240_p4_owns_pi_test_240_p4owns_1778254996734_86767","eventType":"payment_intent.succeeded","paymentIntentId":"pi_test_240_p4owns_1778254996734_86767","reason":"missing_parent_email","metadataKey":"parentEmail","metadataValue":null,"persistedRowId":null},
  {"ts":1778254996882,"eventId":"evt_test_240_p4_owns_pi_test_240_p4owns_1778254996734_86767","eventType":"payment_intent.succeeded","paymentIntentId":"pi_test_240_p4owns_1778254996734_86767","reason":"checkout_session_completed_already_owns","metadataKey":"idempotency_key","metadataValue":"checkout:cs_test_seed_rI_xHoGe","persistedRowId":34}
]}
```

### Branch 4 — `stripe_api_checkout_session_match` (fault-injected) (raw runtime skip entry)

```
webhook ts=2026-05-08T15:43:17.312Z  HTTP 200
{"received":true,"event_type":"payment_intent.succeeded","handled":true,"persistedRowId":35}

skips ts=2026-05-08T15:43:17.325Z  HTTP 200
{"entries":[{"ts":1778254997323,"eventId":"evt_test_240_p4_apimatch_pi_test_240_1778254997312_531494","eventType":"payment_intent.succeeded","paymentIntentId":"pi_test_240_1778254997312_531494","reason":"stripe_api_checkout_session_match","metadataKey":"session.metadata.paymentType","metadataValue":"cart_checkout","persistedRowId":35}]}
```

### Unreachable-in-test annotation (Skip 4: real Stripe API match)

The `stripe.checkout.sessions.list` real-match path at `server/webhook-handler.ts:767-798` cannot be exercised from the regression test without a live Stripe-managed Checkout Session whose metadata signals `cart_checkout`; the fault-injection path at `server/webhook-handler.ts:746-766` exercises the identical `recordTask219Skip` + `console.warn` payload shape. **Tracked under existing task #239** (per Task #240 spec, "Out of scope — Do NOT cover Skip 4… #239 covers it").

**Verdict:** every reachable skip branch in the source emits a structured WARN log via `recordTask219Skip`. The single unreachable-in-test branch is annotated and tracked. ✅

---

## Section 5 — Test run summary

**Exact command used:**

```
$ date -u
Fri May  8 03:43:32 PM UTC 2026

$ PAYMENT_PROCESSOR_ENABLED=true npm run test:server -- --testPathPatterns=cart-pi-persistence-regression
```

This runs `node --experimental-vm-modules node_modules/jest/bin/jest.js --config=jest.server.config.cjs --testPathPatterns=cart-pi-persistence-regression` (`scripts.test:server` from `package.json`). `PAYMENT_PROCESSOR_ENABLED=true` is mandatory — `server/tests/setup.ts:7-13` throws if it is missing. `STRIPE_WEBHOOK_SECRET` is read from the Replit-managed environment so `signWebhook` produces signatures the running dev server accepts. The dev server on port 5000 is reused by `globalSetup.mjs` (no spawn needed). The default `DATABASE_URL` (Replit Helium) is the test DB.

Why this entrypoint: of the three test entrypoints (`npm test`, `npm run test:server`, `npm run test:client`), `test:server` is the only one whose `jest.server.config.cjs` matches `server/tests/**/*.test.ts` and runs the global server-spawn setup. `npm test` would also run `test:client`, which is unrelated to this audit.

**Full output:**

```
> rest-express@1.0.0 test:server
> node --experimental-vm-modules node_modules/jest/bin/jest.js --config=jest.server.config.cjs --testPathPatterns=cart-pi-persistence-regression

[jest globalSetup] reusing existing server at http://localhost:5000
(node:1021) ExperimentalWarning: VM Modules is an experimental feature and might change at any time
(Use `node --trace-warnings ...` to show where the warning was created)
PASS server/tests/integration/payment-flow/cart-pi-persistence-regression.test.ts (20.874 s)
  Task #219: payment_intent.succeeded persistence is exactly-once
    ✓ P1 + P2: persists exactly one row keyed by (stripe_event_id, payment_intent_id) and is idempotent on replay (1407 ms)
    ✓ P3: persistence failure surfaces as HTTP 5xx and a Stripe retry of the same event is NOT silently acked (463 ms)
    ✓ P4: every reachable payment_intent.succeeded skip branch records a structured skip entry at runtime (2056 ms)

Test Suites: 1 passed, 1 total
Tests:       3 passed, 3 total
Snapshots:   0 total
Time:        20.986 s, estimated 26 s
Ran all test suites matching cart-pi-persistence-regression.
```

**Pass rate:** 3/3 = 100%. **Runtime:** 20.986 s wall clock (`PASS … (20.874 s)` for the suite). **Test runner version:** `npx jest --version` → `30.1.2`. **Node version:** `v20.20.0`.

---

## Closure

All four properties are verified against `b244edff4324e371354e871c8d9b8327b8bf9ed6` with raw, timestamped evidence. The remaining audit gap from #219/#238 is closed. Skip 4 (real Stripe API match path) remains tracked under task #239 per scope.
