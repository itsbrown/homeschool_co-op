# Production Balance Triage — Phase 2 Evidence

- **Task ref:** #257
- **Status:** **BLOCKED — prerequisite files missing.** No SQL was run. No production data was read or modified.
- **Environment intended:** production (read-only)
- **Report generated:** 2026-05-12T15:09:35Z
- **Generated by:** task-agent (Task #257)

## Summary

Task #257 instructs the agent to run the read-only Phase 2 SQL queries
defined in `docs/audit/BALANCE_TRIAGE_PRODUCTION.md` against production
and capture the output. Step 0 of the task is a hard pre-flight gate
that requires the following five files to exist before any audit SQL is
executed. **None of them exist on the local checkout, and a full
`git ls-tree -r origin/main` confirms none of them exist on
`origin/main` either.**

Missing files:

- `docs/audit/BALANCE_TRIAGE_PRODUCTION.md`
- `docs/audit/sql/b1-orphan-scheduled-payments.sql`
- `docs/audit/sql/b2-missing-pending-schedules.sql`
- `docs/audit/sql/b4-cached-remaining-vs-computed.sql`
- `docs/audit/sql/b5-pending-sum-by-enrollment.sql`

Per Step 5 of the task spec:

> If any `ls` fails after the pull: **STOP** and report the current
> `HEAD` SHA and `origin/main` SHA back to the user. Do not invent SQL
> or guess column names.

The agent stopped immediately and did not run any queries.

## Git state captured at stop time

| Ref          | SHA                                        |
|--------------|--------------------------------------------|
| local `HEAD` | `c6fb1479426c14d0153c67ed29facced73b42bf0` |
| `origin/main`| `0bc39cca8e01aeba06ebec25584e211a7e797f8f` |

`HEAD` is behind `origin/main` by several commits, but the missing
files are **not** present on `origin/main` either, so pulling would not
unblock this task. (`git pull origin main` was not run: it timed out on
the first attempt waiting on credentials, and the agent's rules of
engagement disallow git mutations on this branch.)

## What `docs/audit/` actually contains today

```
201-evidence/
201-reallocator-tests-report.md
212-membership-balance-source-report.md
219-stop-payments-disappearing-report.md
222-refund-durability-report.md
242-schema-migration-fix-report.md
246-payment-allocations-fk-target-report.md
247-evidence/
247-missing-membership-enrollment-id-report.md
248-evidence/
248-membership-enrollment-id-backfill-report.md
README.md
```

There is no `docs/audit/sql/` directory, and no
`BALANCE_TRIAGE_PRODUCTION.md`. A repo-wide
`git ls-tree -r origin/main --name-only | rg -i "balance_triage|b1-orphan|b2-missing|b4-cached|b5-pending|docs/audit/sql"`
returns zero matches.

## Action required from the user

One of the following must happen before Task #257 can be re-run:

1. Land the missing triage doc + SQL files on `main` (this would
   normally come out of a Phase 1 / Part A planning task). Once they
   exist on `origin/main`, re-open Task #257 and the agent can execute
   the read-only run as written.
2. Or: replace Task #257 with a Phase 1 task whose deliverable is to
   *author* `docs/audit/BALANCE_TRIAGE_PRODUCTION.md` and the four SQL
   files, then re-queue Task #257 afterwards.

The agent intentionally did not author the SQL itself: the schema
references in the task (e.g. `program_enrollments`, `scheduled_payments`,
"cached vs effective" wording) require column-exact queries, and the
task explicitly says "Do not invent SQL or guess column names."

## Confirmation: no production access occurred

- No `psql`, no `executeSql`, no DB skill call against
  `environment: "production"` was issued from this task.
- No `UPDATE` / `DELETE` / `INSERT` against any database.
- Only filesystem operations: `ls`, `git rev-parse`, `git ls-tree`,
  `git log`, and writing this README.
